// Intentionally empty. Tailwind v4 is applied via the @tailwindcss/vite
// plugin in vite.config.ts, not via PostCSS. This file exists only so
// Vite/PostCSS never walks up and picks up an unrelated config from a
// parent folder on your machine.
module.exports = {};
